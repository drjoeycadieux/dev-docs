# Netlify configuration for faang-dev-learning

[build]
  # Directory with the Vite project (the nested folder)
  base = "faang-dev-learning"
  
  # Command to build the project
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
